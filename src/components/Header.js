import React, { Component } from 'react';

export class Header extends Component {
    render() {
        const display = 'header';
        return (
            <div>
                { display }
            </div>
        );
    }
}